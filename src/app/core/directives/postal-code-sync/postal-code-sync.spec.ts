import { AddressLogic } from './postal-code-sync';

describe('AddressLogic', () => {
  it('should create an instance', () => {
    const directive = new AddressLogic();
    expect(directive).toBeTruthy();
  });
});
